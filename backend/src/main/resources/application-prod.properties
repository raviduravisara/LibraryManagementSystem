# Railway Production Configuration
# Do NOT set spring.profiles.active inside a profile-specific file.
# The active profile should be provided via environment variable (SPRING_PROFILES_ACTIVE).

# MongoDB Atlas Configuration (using your existing database)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://malika:Malika12345@cluster0.pu2hdjk.mongodb.net/LibraryDatabase?retryWrites=true&w=majority&ssl=true&connectTimeoutMS=30000&socketTimeoutMS=30000&serverSelectionTimeoutMS=30000}
spring.data.mongodb.database=${MONGODB_DATABASE:LibraryDatabase}

# Server Configuration
server.port=${PORT:8081}

# CORS Configuration for Vercel Frontend
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,https://sarasavi-library-management.vercel.app,https://sarasavi-library-management-o1eofuyqz.vercel.app}

# Spring Security Configuration
spring.security.user.name=${ADMIN_USERNAME:admin}
spring.security.user.password=${ADMIN_PASSWORD:admin123}
spring.security.user.roles=USER

# Email Configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:ktharanga128@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:cadz sweb uery flbv}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Application Configuration
app.member.id.prefix=LIB
app.member.id.year=2025

# Logging
logging.level.com.management.library=INFO
logging.level.org.springframework.data.mongodb=INFO